 // метод для нахождения слов на букву "а" и длинной 3 символа
    public static List<String> search(List<String> list) throws NullElementException {
        return list.stream().
                filter(string ->
                {
                    try {
                        if (string != null) return string.length() == 3;
                        else throw new NullElementException();
                    } catch (NullElementException e) {
                        System.out.println("нулевой элемент");
                        return Boolean.parseBoolean(null);
                    }
                }).
                filter(s -> s.startsWith("a")).
                collect(Collectors.toList());
    }
